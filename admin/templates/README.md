The admin dashboard renders small HTML views directly from `admin/app.py`.

This directory is kept for template authors who want to replace the inline
views with file-backed templates without changing the Railway service layout.
